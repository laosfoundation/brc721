# Example config to test against a local regtest node (Docker compose setup)
BITCOIN_RPC_URL=http://127.0.0.1:18443
BITCOIN_RPC_USER=dev
BITCOIN_RPC_PASS=dev
# Alternatively, cookie auth (if you disable rpcuser/rpcpassword and enable cookie-based auth):
# BITCOIN_RPC_COOKIE=$(pwd)/docker/bitcoin/regtest/.cookie

# Optional: path to SQLite database (defaults to ./.brc721/brc721.sqlite)
# BRC721_DB_PATH=.brc721/brc721.sqlite

# HTTP API server bind (defaults to 127.0.0.1:8080)
# BRC721_API_BIND=0.0.0.0:8080

# HTTP API auth token (if set, clients must send x-api-key or Authorization: Bearer)
# BRC721_API_TOKEN=changeme
